# Copie este arquivo para .env e ajuste os valores localmente.
# Em dev, aponta pro backend rodando local; em produção, é configurado
# direto nas Environment Variables do Vercel (não vem deste arquivo).
VITE_API_URL=http://localhost:3389
